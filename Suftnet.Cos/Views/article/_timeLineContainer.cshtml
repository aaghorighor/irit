@model List<Suftnet.Cos.DataAccess.TopicDto>

    @{
        int index = 0;
        foreach (var item in Model)
        {
            index = index + 1;         
            Html.RenderPartial("_step" + index, item);
        }        
    }

